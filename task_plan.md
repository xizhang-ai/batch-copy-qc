# 批量种草文案 QC 独立产品：规划任务

## 目标

按照已确认的完整实施计划，构建并验收一个可在 Windows 本地运行的独立批量种草文案 QC 产品，覆盖项目/帖子类型 Brief、爆款参考、批量生成、自动 QC、人工审核、飞书子 Sheet 输出和完整审计链。

## 当前阶段

- 状态：长任务实施中
- 阶段：并行执行 Task 1–20，随后由主智能体集成 Task 21–22
- 编码边界：保持独立产品与轻量单体；子智能体按目录所有权工作，主智能体负责交叉集成、实网适配和最终验收。

## 阶段

- [x] 阶段 1：确认产品边界与已冻结决策
- [x] 阶段 2：核对现有短策划、技术选型和视觉参考
- [x] 阶段 3：锁定文件结构、领域模型、状态机与接口边界
- [x] 阶段 4：编写逐任务实施计划
- [x] 阶段 5：把爆款参考类型补入需求、数据、API、QC、前端和验收后完成自检
- [x] 阶段 6：形成长任务执行交接点并停止，不启动编码
- [ ] 阶段 7：初始化独立仓库和应用骨架
- [ ] 阶段 8：完成后端领域、持久化、模型/QC/审核/飞书 API
- [ ] 阶段 9：完成前端项目、类型、规则、看板、审核和输出页面
- [ ] 阶段 10：全栈联调、实网验证、E2E、Windows 启动与文档收口
- [ ] 阶段 11：独立审查官终审与问题修复

## 已冻结决策

- 产品是独立产品，位于 `E:\xixiAi\batch-copy-qc`，只适量参考其他项目。
- 技术栈：React + TypeScript + Vite；Python 3.12 + FastAPI + Pydantic；SQLite；RapidFuzz。
- 用户输入项目 Brief；模型拆解后可视化，所有字段可修改。
- 文案类型无内置预设，由用户新增空白类型，可再次输入或上传类型 Brief。
- 帖子类型也可由用户提供 1–5 篇爆款参考帖建立；系统完整保存原帖，AI 抽取可编辑风格画像，生成 Prompt 同时携带原帖与画像，但不得继承来源事实或照搬独特原句。
- 用户未单独输入类型 QC 时，“一定要有/一定不要有”自动成为可编辑的默认类型 QC。
- 模板入口仅预留数据边界，本阶段不做模板库。
- QC 分项目级、类型级、生成后的个例人工处理；硬规则不可覆盖，软规则允许类型覆盖项目。
- AI QC 通过可直接进入已完成；异常项才进入人工审核。
- 自动 QC 发起的 AI 修改完成后重新 AI QC；人工发起的定向修改完成后返回人工审核。
- 强制通过必须记录非空遗留问题。
- 每次飞书输出在同一电子表格中新建子 Sheet；API 与飞书标识由 `.env` 提供。
- 模型 API 供应商固定为 CLIPROXY；Base URL、API Key、生成/QC 模型与超时从 `.env` 读取，开发测试保留 Fake Adapter。
- 首版不做拖拽，不设置“一轮修改/二轮修改”列，修改次数作为元数据。
- 内容质量优化属于 P1，本计划先搭建可运行、可验证的轻量框架。

## 错误与风险记录

| 项目 | 状态 | 处理 |
|---|---|---|
| 早期方案曾过度向另一个 TypeScript 项目对齐 | 已纠正 | 保持独立 Python/FastAPI 产品，仅参考交互与接口模式 |
| 视觉规范中“人工审核是唯一出口”与最新流程冲突 | 已纠正 | 以最新决定为准：AI QC 通过可直接完成 |
| 参考案例画像与“原帖必须进入 Prompt”存在早期口径差异 | 已纠正 | 原帖完整保存并进入 Prompt，同时保留画像、事实隔离和防照搬 QC |
| PowerShell 将 if/foreach 结果直接接管道时出现空管道解析错误 | 已解决 | 先赋值到 `$results`，再单独送入 `Format-Table`；后续脚本沿用此模式 |
| 首次 Git 提交缺少 author identity | 已解决 | 仅为本仓库设置 `Codex <codex@local>`，不修改用户全局 Git 配置 |
