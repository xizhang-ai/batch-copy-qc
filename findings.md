# 发现与决策依据

## 使用说明

记录本次完整计划编写过程中，从已有文档、视觉参考和技术资料中提取的事实。外部资料只进入本文件，不进入高频注入的任务计划。

## 已知事实

- 现有短策划完整覆盖了项目 Brief、类型 Brief、三级 QC、硬/软规则、个例编辑、飞书子 Sheet 和失败重试，但部分流程口径已被后续决定替代。
- 现有技术选型确认独立产品采用 React + TypeScript + Vite、Python 3.12 + FastAPI + Pydantic v2、SQLite、RapidFuzz、HTTPX、pytest 和 Playwright。
- 权威存储使用 SQLite；JSON/CSV 只能作为导出或诊断产物，不能作为恢复与并发控制依据。
- P0 Brief 输入以粘贴文本、`.txt`、`.md`、`.docx` 为必做；PDF/OCR/Excel 推迟，避免文档解析扩大首版范围。
- 视觉稿采用 72px 左侧图标栏、48px 顶部悬浮导航、五列粉彩看板、轻阴影白色任务卡和约 68% 视口宽的人工审核覆盖面板。
- 看板视觉稿的统计关系可用作验收样例：总数等于五列合计，AI 处理中等于 AI QC 中与 AI 修改中合计。
- 人工审核视觉稿明确左侧正文编辑、右侧 QC 结果、选中文字触发 AI 修改、底部四个固定操作。
- 视觉稿中的文案示例、品牌、数量和“人工审核是最终出口”文案不是业务事实；实现必须使用数据，并以最新状态机为准。

## 需要从旧文档修正的口径

1. AI QC 通过后直接进入 `completed`，不再要求每条文案人工确认。
2. 自动 QC 触发的改写完成后回到 AI QC；人工发起的选区改写完成后回到人工审核。
3. “待人工审核”只接收硬规则冲突、事实不确定、相似度决策、低置信度、自动重试耗尽、API 异常和人工召回等异常项。
4. 默认飞书输出只包含 `completed` 条目；未通过或仍待人工处理的条目不进入正式输出。
5. `AI 自动通过`、`人工通过`、`强制通过 · 有遗留问题`是三个完成原因，不应与看板运行状态混成同一字段。
6. 首版不做复杂自动保存版本系统；项目配置使用明确保存，文案内容版本和审核事件必须留痕。
7. 首版无拖拽；状态由工作流动作推进，避免非法跨列移动。

## 视觉基线

- 看板参考：`C:\Users\xicandy\.codex\generated_images\01a01fa0-f6f5-7a41-bb9f-ed653eda14d1\exec-48a69b37-2716-444e-95c0-1e5570704981.png`
- 人工审核参考：`C:\Users\xicandy\.codex\generated_images\01a01fa0-f6f5-7a41-bb9f-ed653eda14d1\exec-3fb30fcf-7e14-419b-be05-8721e5c2c042.png`
- 颜色、字体、间距、圆角、阴影、响应式和可访问性以“种草文案 QC 前端视觉规范 v0.1”为实现基线。

## 2026-08-21 新增：爆款文案参考建立帖子类型

- 帖子类型除空白/Brief 建立外，增加“从爆款文案参考创建”。
- P0 同一帖子类型支持 1–5 篇完整参考帖，可粘贴全文或上传 txt/md/docx；不抓取小红书链接。
- AI 从参考中提取标题钩子、开头方式、结构节拍、叙事视角、语气、人设、场景、信息密度、结尾与标签策略，形成可编辑的类型草稿。
- 参考文案中的品牌、产品事实、价格、功效和个人经历不能自动进入当前项目事实。
- 原帖必须完整保存；生成阶段同时把原帖全文与用户确认后的参考风格画像放入模型 Prompt，并明确标记当前项目事实优先、来源事实禁止继承、独特原句禁止照搬。
- RapidFuzz 除批次内相似度外，还检查生成稿与原爆款参考的标题、开头和全文相似度；过近时进入自动改写或人工处理。
- UI 统一使用“帖子类型”；代码与数据库内部仍可使用稳定术语 `copy_type`。

### 从参考界面吸收的产品逻辑

- 类型建立依据不是互斥的“创建模式”，而是可以组合勾选的 `参考案例` 与 `描述要求`。
- 参考案例按完整帖子保存标题、正文和话题，保持标题与正文配对；一篇即可建立类型，同一类型最多 5 篇。
- 描述要求用于明确标题方向、正文结构、语气、人设、场景等不能只靠案例稳定推断的内容。
- `一定要有` 与 `一定不要有` 是用户明确输入的类型约束，优先于 AI 从案例推断的风格画像，但仍不能覆盖项目硬规则；用户未单独输入类型 QC 时，它们自动生成可见、可编辑的默认类型 QC。
- UI 不照搬截图的重表单风格，仍使用本产品的轻量粉彩视觉体系和两层视觉海拔。

## 2026-08-21 模型供应商

- 用户确认模型 API 使用 CLIPROXY。
- 独立产品实现 `CliProxyModelAdapter`，不依赖另一产品运行时。
- `.env` 提供 CLIPROXY Base URL、API Key、生成模型、QC 模型、reasoning effort 与 timeout。
- 开发和离线验收保留确定性 Fake Adapter；真实 adapter 与 Fake 共享六方法 contract tests。
- 参考现有 CLIPROXY 调用模式使用 OpenAI Responses 兼容请求；最终接入时仍以用户实际 Base URL 和返回样例进行合同验证。

## 2026-08-21 长任务集成准备

- 已初始化独立 Git 仓库 `E:\xixiAi\batch-copy-qc\.git`；父目录及 `New project 2` 不在版本控制范围内。
- `New project 2\content-hub` 是更重的 Next/多包 TypeScript 工程，包含 Drizzle、Worker、素材库和大量既有模块；本产品保持 React/Vite + FastAPI，不复制其框架与业务代码。
- 只适量参考其 Windows 开发日志隔离、前后端代理验证和 Playwright 组织方式；实际脚本需按本产品单进程 FastAPI 交付重新实现。
- 当前真实外部配置已通过只读 smoke test：CLIPROXY 两个模型、飞书 tenant token、Wiki 节点解析、电子表格工作表列表均成功。真实写入留到 exporter 完成后用受控测试验证。
- Windows PATH 上的系统 Python 是 3.14.0，与锁定的 `>=3.12,<3.13` 不符；Codex bundled Python 为 3.12.13，长任务测试和安装统一使用其绝对路径。Node PATH 为 24.x，可满足 Vite/Playwright。

## 2026-08-21 实现与终审结论

- 领域持久化按全部 14 张计划实体表实现，另含 `schema_migrations`；旧 001 数据库通过独立 002 迁移增加飞书幂等字段。
- CLIPROXY 默认严格 Responses；只有显式 `auto` 才在 404/405/501 回退 Chat，400/422 不回退。
- 真实 CLIPROXY 解析调用已通过结构验证；一次样例把禁止宣称归类不够理想，属于已冻结的内容质量 P1，不影响框架与合同放行。
- 飞书 export run 保存不可变行快照/hash、确定性安全 Sheet 名和历史最大写入行；同 ID 不同 payload 冲突，重试会对账并清理旧尾行。
- 飞书当前并发幂等边界是单进程/单事件循环，因此生产脚本强制 `--workers 1`；未来扩展多 worker 时需数据库 lease/CAS。
- 类型 Brief 解析结果会回填可编辑类型字段；识别到的项目事实只作为 `project_change_suggestions` 显示，不静默覆盖已确认项目内容。
- Windows PowerShell 5 对 UTF-8 无 BOM 脚本不可靠；交付脚本使用 ASCII 提示，中文仍保留在产品 UI 和 Markdown 文档。
- E2E 使用 Fake Adapter，保证测试不会产生模型费用或飞书写入；真实连接另走最小 smoke test。
- 类型 Brief 审阅使用独立 `brief_review` 数据，不混入生成 requirements；建议和冲突逐条保存内容、来源、置信度与 pending/confirmed/ignored 决定，旧库通过 003 迁移保留数据。
- QC 规则边界最终确定：project/type hard 同类累加；type soft 覆盖同类 project soft；type soft 试图覆盖 project hard 时产生冲突并阻止生成。
- 人工直接编辑与选区改写都会同步重新执行确定性、批次/参考相似度和语义 QC；只有复检后已消失的问题会解决，模型失败必须保留旧 finding 并阻止普通通过。
- 强制通过的“遗留问题”和“放行理由”是两个独立非空字段；前端、后端审核事件和飞书导出使用同一完成原因语义。
- FastAPI 请求不能与 worker 共享一个 `check_same_thread=False` SQLite 连接；最终实现采用请求级独立连接和 worker 专用连接，并以并发读写回归锁定该边界。
- 最终独立终审结论为 PASS：后端 100 项、前端 15 项、Mock E2E 5 条、真实栈 E2E 1 条全部通过，生产包与 secrets 检查通过。

## 2026-08-24：对话驱动与三篇预览改造决策

- 用户确认当前配置流程过长，希望探索类似“左侧对话、右侧任务工作区”的简单入口；对话不是唯一 UI，详细项目/类型/QC 能力保留为高级配置。
- 对话只产生可见的结构化建议和待用户点击应用的动作；不允许模型静默修改事实、规则、数量或开始生成。
- 默认先生成 3 篇预览。多类型时预览按类型顺序先各取 1 篇，再轮询补满 3 篇，确保具有代表性且可重复验收。
- 用户认可后，剩余文案必须加入同一个 generation run，而不是创建第二批；这样版本、QC、审核与飞书导出链条保持统一。
- 当前后端的 generation run 已使用冻结配置快照和 item slot；通过为 run 增加 preview phase、并在确认时创建缺失 slot，即可实现同批继续，不需要重写 QC worker。
- 看板前端已先调整为三个用户组：系统处理中（pending_ai_qc/ai_qc_running/ai_rewrite_running）、需人工处理（human_review）、已完成（completed）；内部状态保持不变。
